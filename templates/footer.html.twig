<footer class="hidden md:text-center md:p-6 md:mt-20 md:bg-gray-900 md:text-white">
	<div class="max-w-screen-xl flex flex-wrap items-center mx-auto p-4 justify-center">
		<div class="block w-auto" id="navbar-default">
			<ul class="font-medium flex flex-row space-x-8 rtl:space-x-reverse">
				<li>
					<a href="{{ path('app_accueil') }}" class="block py-2 px-3 rounded text-gray-400 hover:text-blue-700" aria-current="page">Accueil</a>
				</li>
				<li>
					<a href="{{ path('app_sio') }}" class="block py-2 px-3 rounded text-gray-400 hover:text-blue-700">BTS SIO</a>
				</li>
				<li>
					<a href="{{ path('app_stage') }}" class="block py-2 px-3 rounded text-gray-400 hover:text-blue-700">Éxpérience Pro.</a>
				</li>
				{# <li>
					<a href="{{ path('app_competence') }}" class="block py-2 px-3 rounded text-gray-400 hover:text-blue-700' }}">Compétences</a>
				</li> #}
				<li>
					<a href="{{ path('app_contact') }}" class="block py-2 px-3 rounded text-gray-400 hover:text-blue-700">Me contacter</a>
				</li>
			</ul>
		</div>
	</div>
</footer>
